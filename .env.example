# Production env template for docker-compose.
# Copy this file to .env.production, fill in the secrets, and keep it out of version control.

NODE_ENV=production

##############################
# Gateway + shared settings
##############################
GATEWAY_PORT=5050
PORT=5050
CORS_ORIGINS=https://explorefusion.online,https://www.explorefusion.online
GATEWAY_URL=http://gateway:5050
JWT_SECRET=

# Lifecycle note: post, social, chat, matches, and notification APIs are served by the gateway (e.g., /api/v1/posts, /api/v1/follow, /api/v1/matches).

AI_PROVIDER=groq
GROQ_API_KEY=

IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_URL_ENDPOINT=

##############################
# MongoDB databases (shared cluster)
##############################
AUTH_MONGO_URI=mongodb://mongo:27017/auth
BOOKING_MONGO_URI=mongodb://mongo:27017/booking
POST_MONGO_URI=mongodb://mongo:27017/post
MATCHES_MONGO_URI=mongodb://mongo:27017/matches
SOCIAL_MONGO_URI=mongodb://mongo:27017/social
# Optional fallback for services that still look for MONGO_URI
MONGO_URI=

##############################
# Client/Vite build-time variables
##############################
VITE_API_BASE_URL=http://gateway:5050/api/v1
VITE_IMAGEKIT_PUBLIC_KEY=
VITE_IMAGEKIT_URL_ENDPOINT=
VITE_IMAGEKIT_AUTH_ENDPOINT=http://gateway:5050/imagekit-auth
VITE_BRAND_LOGO_URL=
