# Client environment variables
# Copy to `client/.env` for local development.

# Backend/Gateway API base URL.
# You can set either:
# - full API base: https://explore-fusion-gateway.onrender.com/api/v1
# - OR just the origin: https://explore-fusion-gateway.onrender.com
# The app will normalize it to end with `/api/v1`.
VITE_API_BASE_URL=

# ImageKit client-side settings (public values).
VITE_IMAGEKIT_PUBLIC_KEY=
VITE_IMAGEKIT_URL_ENDPOINT=

# Do NOT set VITE_IMAGEKIT_AUTH_ENDPOINT unless you really need to override it.
# By default it uses `${VITE_API_BASE_URL}/imagekit-auth`.

# Optional: hosted logo URL (e.g., ImageKit URL)
# If unset, the app falls back to `/branding/logo.png`
VITE_BRAND_LOGO_URL=

# Example (once uploaded):
# VITE_BRAND_LOGO_URL=https://ik.imagekit.io/Ritesh5001/explore-fusion/branding/logo.png
