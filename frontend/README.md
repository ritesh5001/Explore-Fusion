# WanderMatch Web

React + TypeScript + Vite website for the WanderMatch MERN app.

```bash
cp .env.example .env.local
npm install
npm run dev
```

The app reads `VITE_API_URL` and defaults to `http://localhost:4000`.
