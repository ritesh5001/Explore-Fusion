# Copy to `.env` for local development.
# Do not commit real secrets.

NODE_ENV=development

# Gateway
PORT=5050
CORS_ORIGINS=http://localhost:5173
JWT_SECRET=
AUTH_MONGO_URI=mongodb://localhost:27017/auth
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_URL_ENDPOINT=

# Core services (required in production)
BOOKING_MONGO_URI=mongodb://localhost:27017/booking
MATCHES_MONGO_URI=mongodb://localhost:27017/matches
POST_MONGO_URI=mongodb://localhost:27017/post
SOCIAL_MONGO_URI=mongodb://localhost:27017/social

# Notes:
# - Admin APIs are hosted directly by the gateway at /api/v1/admin, so no extra URL is needed.
# - AI APIs are also served at /api/v1/ai; configure the GROQ keys (`GROQ_API_KEY`, `AI_PROVIDER`) through the root `.env`.
# - In local dev, the gateway already falls back to localhost ports if these aren't set.
# - In production (Render), set the URLs to the deployed service base URLs.
