# WanderMatch Mobile

Expo React Native app for iOS and Android.

```bash
cp .env.example .env
npm install
npm run start
```

The app reads `EXPO_PUBLIC_API_URL` and defaults to `http://localhost:4000`. For Android Emulator, set it to `http://10.0.2.2:4000`.
