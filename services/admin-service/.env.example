# Copy to .env for local dev (do not commit secrets)

PORT=5007
NODE_ENV=production
JWT_SECRET=<SAME_AS_AUTH_SERVICE_JWT_SECRET>

# Option A (recommended): two DB URIs
AUTH_MONGO_URI=<MONGODB_ATLAS_URI_FOR_AUTH_DB>
BOOKING_MONGO_URI=<MONGODB_ATLAS_URI_FOR_BOOKING_DB>
