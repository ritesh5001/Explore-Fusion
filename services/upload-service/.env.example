# Copy to `.env` for local development.
# Upload service is used for `/uploads` static hosting + optional ImageKit-backed uploads.

NODE_ENV=development
PORT=5005
# Optional alternate port (server.js reads PORT first)
UPLOAD_PORT=5005

# Required for ImageKit auth / upload integration
IMAGEKIT_PUBLIC_KEY=<YOUR_IMAGEKIT_PUBLIC_KEY>
IMAGEKIT_PRIVATE_KEY=<YOUR_IMAGEKIT_PRIVATE_KEY>
IMAGEKIT_URL_ENDPOINT=<YOUR_IMAGEKIT_URL_ENDPOINT>

# Optional: some services validate via gateway; upload-service does not require this by default.
GATEWAY_URL=http://localhost:5050
